[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "openshorts"
version = "0.1.0"
description = "CLI for OpenShorts: clip long videos into vertical shorts and publish them, from the terminal"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "OpenShorts" }]
keywords = ["video", "clips", "shorts", "ai", "tiktok", "youtube-shorts", "cli"]
classifiers = [
  "Environment :: Console",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Multimedia :: Video",
]

[project.urls]
Homepage = "https://github.com/mutonby/openshorts"
Source = "https://github.com/mutonby/openshorts"
Documentation = "https://github.com/mutonby/openshorts#api-mcp-and-cli"

[project.scripts]
openshorts = "openshorts_cli:main"

[tool.setuptools]
py-modules = ["openshorts_cli"]
