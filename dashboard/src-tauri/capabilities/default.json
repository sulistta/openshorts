{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the primary OpenShorts desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
